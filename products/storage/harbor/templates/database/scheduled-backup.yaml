apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: azure-harbor-db
spec:
  cluster:
    name: {{ .Values.database.clusterName }}
  schedule: '0 23 4 * * *'
  backupOwnerReference: self
  method: plugin
  pluginConfiguration:
    name: barman-cloud.cloudnative-pg.io
