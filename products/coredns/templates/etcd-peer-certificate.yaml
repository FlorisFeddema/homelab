apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name:  coredns-etcd-peer
spec:
  isCA: false
  secretName: coredns-etcd-peer
  usages:
    - server auth
  issuerRef:
    name: coredns-issuer
    kind: Issuer
    group: cert-manager.io

