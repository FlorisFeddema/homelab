{{- if and .Values.route.enabled .Values.route.auth.enabled }}
apiVersion: batch/v1
kind: Job
metadata:
  name: kubernetes-mcp-server-secret-init
  namespace: {{ .Release.Namespace }}
  annotations:
    "helm.sh/hook": pre-install,pre-upgrade
    "helm.sh/hook-delete-policy": before-hook-creation,hook-succeeded
spec:
  activeDeadlineSeconds: 120
  backoffLimit: 3
  template:
    spec:
      restartPolicy: OnFailure
      serviceAccountName: kubernetes-mcp-server-secret-init
      containers:
        - name: bearer-auth-secret-init
          image: "{{ .Values.route.auth.secretInit.image.repository }}:{{ .Values.route.auth.secretInit.image.tag }}"
          imagePullPolicy: IfNotPresent
          command:
            - sh
            - -c
            - |
              kubectl get secret kubernetes-mcp-bearer-auth-secret -n {{ .Release.Namespace }} >/dev/null 2>&1 || \
              kubectl create secret generic kubernetes-mcp-bearer-auth-secret -n {{ .Release.Namespace }} --from-literal=mcp-client="$(head -c 32 /dev/urandom | od -An -tx1 | tr -d ' \n')"
{{- end }}
