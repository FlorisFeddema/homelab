{{ if .Values.backups.enabled }}
apiVersion: longhorn.io/v1beta1
kind: RecurringJob
metadata:
  name: backup-job
  namespace: longhorn-system
spec:
  cron: 0 */2 * * *
  task: "backup"
  groups:
    - default
  retain: 4
  concurrency: 4
{{ end }}

