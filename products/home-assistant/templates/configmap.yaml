apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ template "homeAssistant.fullname" . }}-config
data:
  configuration.yaml: |- {{ include "common.tplvalues.render" ( dict "value" .Values.configuration "context" $) | nindent 4 }}
