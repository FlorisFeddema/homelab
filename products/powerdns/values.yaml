fullnameOverride:
nameOverride:
commonlabels:

replicas: 1

image:
  registry: docker.io
  repository: powerdns/pdns-auth-48
  tag: 4.8.3

resources: {}

powerdns:
  database:
    postgres:
      existingSecret: "powerdns-credentials" #key = postgres-password
      host: 'platform-pgbouncer.pgo.svc'
      port: 5432
      databaseName: 'powerdns'
      user: 'powerdns'
  apiKey:
    existingSecret: "powerdns-credentials" #key = apiKey

admin:
  replicas: 1

  image:
    registry: docker.io
    repository: powerdnsadmin/pda-legacy
    tag: 0.3.0

  config:
    existingSecret: "powerdns-admin-credentials" #keys = adminKey, databaseConnectionString

  ingress:
    domain: powerdns.cloud.feddema.dev
