{{ if .Values.recursor.secret.sealedValues }}
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: {{ .Values.recursor.secret.name }}
spec:
  encryptedData:
    apiKey: {{ .Values.recursor.secret.sealedValues.apiKey }}
  template:
    metadata:
      name: {{ .Values.recursor.secret.name }}
{{ end }}
