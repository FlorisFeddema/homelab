apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: {{ template "powerdns.name" . }}-admin-postgres
spec:
  instances: {{ .Values.admin.database.replicas }}
  storage:
    size: {{ .Values.admin.database.size }}
