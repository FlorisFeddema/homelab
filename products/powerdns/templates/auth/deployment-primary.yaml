apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ template "powerdns.fullname" . }}-auth-primary
  labels:
    app.kubernetes.io/name: {{ template "powerdns.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/component: "powerdns-auth-primary"
    {{- include "labels" . | nindent 4 }}
spec:
  revisionHistoryLimit: 0
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ template "powerdns.name" . }}
      app.kubernetes.io/instance: {{ .Release.Name }}
      app.kubernetes.io/component: "powerdns-auth-primary"
      {{- include "labels" . | nindent 6 }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ template "powerdns.name" . }}
        app.kubernetes.io/instance: {{ .Release.Name }}
        app.kubernetes.io/component: "powerdns-auth-primary"
        {{- include "labels" . | nindent 8 }}
    spec:
      containers:
        - name: powerdns-auth-primary
          image: "{{ .Values.auth.image.registry }}/{{ .Values.auth.image.repository }}:{{ .Values.auth.image.tag }}"
          imagePullPolicy: IfNotPresent
          env:
            - name: TEMPLATE_FILES
              value: "pdns"
            - name: PDNS_AUTH_API_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.auth.config.apiKey.existingSecret }}
                  key: apiKey
            - name: POWERDNS_POSTGRES_HOST
              value: "{{ .Values.auth.primary.postgres.host }}"
            - name: POWERDNS_POSTGRES_PORT
              value: "{{ .Values.auth.primary.postgres.port }}"
            - name: POWERDNS_POSTGRES_USERNAME
              value: "{{ .Values.auth.primary.postgres.user }}"
            - name: POWERDNS_POSTGRES_DATABASE
              value: "{{ .Values.auth.primary.postgres.databaseName }}"
            - name: POWERDNS_POSTGRES_PASSSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.auth.primary.postgres.existingSecret }}
                  key: postgres-password
          ports:
            - name: api
              containerPort: 8081
              protocol: TCP
          resources: {{ toYaml .Values.auth.primary.resources | nindent 12 }}
          volumeMounts:
            - name: custom-config
              mountPath: /etc/powerdns/templates.d
            - name: default-config
              mountPath: /etc/powerdns/pdns.conf
              subPath: pdns.conf
          securityContext:
            capabilities:
              drop:
                - ALL
            allowPrivilegeEscalation: false
            seccompProfile:
              type: RuntimeDefault
      volumes:
        - name: custom-config
          configMap:
            name: {{ template "powerdns.fullname" . }}-auth-config
            items:
              - key: pdns.j2
                path: pdns.j2
        - name: default-config
          configMap:
            name: {{ template "powerdns.fullname" . }}-auth-config
            items:
              - key: pdns.conf
                path: pdns.conf
