apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: authentik-embedded-outpost
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-dns
spec:
  rules:
    - host: proxy.authentik.cloud.feddema.dev
      http:
        paths:
          - path: /outpost.goauthentik.io
            pathType: Prefix
            backend:
              service:
                name: ak-outpost-authentik-embedded-outpost
                port:
                  number: 9000
  tls:
    - hosts:
        - proxy.authentik.cloud.feddema.dev
      secretName: proxy-authentik-tls
