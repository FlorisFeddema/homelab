apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ template "adguard.fullname" . }}-config
data:
  AdGuardHome.yaml: |-
    dns:
      upstream_dns:
        - powerdns-recursor-dns.powerdns.svc
      fallback_dns:
        - 1.1.1.1
        - 1.0.0.1
