{{ if .Values.secret.sealedValue }}
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: {{ .Values.secret.name }}
spec:
  encryptedData:
    PASSWORD: {{ .Values.secret.sealedValue }}
  template:
    metadata:
      name: {{ .Values.secret.name }}
{{ end }}
