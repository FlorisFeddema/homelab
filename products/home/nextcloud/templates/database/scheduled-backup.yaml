apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: azure-nextcloud-db
spec:
  cluster:
    name: {{ .Values.database.clusterName }}
  schedule: '23 4 * * * *'
  backupOwnerReference: self
  method: plugin
  pluginConfiguration:
    name: barman-cloud.cloudnative-pg.io
