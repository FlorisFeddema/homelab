apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: nextcloud-postgres
spec:
  selector:
    matchLabels:
      "cnpg.io/cluster": nextcloud-postgres
  podMetricsEndpoints:
    - port: metrics
