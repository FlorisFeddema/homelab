{{- if .Values.secret.apiTokenSealed -}}
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: {{ .Values.secret.name }}
  namespace: {{ .Release.Namespace }}
spec:
  encryptedData:
    ARGOCD_API_TOKEN: {{ .Values.secret.apiTokenSealed }}
  template:
    metadata:
      name: {{ .Values.secret.name }}
      namespace: {{ .Release.Namespace }}
    type: Opaque
{{- end }}
