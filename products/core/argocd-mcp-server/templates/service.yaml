apiVersion: v1
kind: Service
metadata:
  name: argocd-mcp-server
spec:
  ports:
    - name: http
      port: {{ .Values.argocdMcp.port }}
      targetPort: http
  selector:
    app.kubernetes.io/name: argocd-mcp-server
