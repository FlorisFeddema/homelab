infro:
  replicas: 1

  nodeSelector:
    kubernetes.io/arch: amd64

  resources:
    requests:
      memory: 128Mi
      cpu: 50m
    limits:
      memory: 512Mi

  config:
    existingSecretName: infro-secrets
