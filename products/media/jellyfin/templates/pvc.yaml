apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ template "common.fullname" }}-media
  labels:
    {{- include "common.labels" . | nindent 4 }}
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: {{ .Values.persistance.media.size }}
