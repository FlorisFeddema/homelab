opentelemetry-operator:
  manager:
    resources:
      limits:
        cpu: null
        memory: 128Mi
      requests:
        cpu: 100m
        memory: 64Mi

    serviceMonitor:
      enabled: true

    prometheusRule:
      enabled: true
      defaultRules:
        enabled: true
