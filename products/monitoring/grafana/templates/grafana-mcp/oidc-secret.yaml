{{- if and .Values.oidc.enabled .Values.oidc.clientSecretSealed }}
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: {{ .Values.oidc.secretName }}
  namespace: {{ .Release.Namespace }}
spec:
  encryptedData:
    client-secret: {{ .Values.oidc.clientSecretSealed }}
  template:
    metadata:
      name: {{ .Values.oidc.secretName }}
      namespace: {{ .Release.Namespace }}
{{- end }}
