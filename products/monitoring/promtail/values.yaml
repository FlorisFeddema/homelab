promtail:
  resources:
    limits:
      memory: 386Mi
    requests:
      cpu: 30m
      memory: 128Mi

  revisionHistoryLimit: 0

  serviceMonitor:
    enabled: true

  config:
    logLevel: warn
    clients:
      - url: http://loki-gateway.loki/loki/api/v1/push
