{{ if and .Values.mongodb.enabled .Values.mongodb.secret.sealedValue }}
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: {{ .Values.mongodb.secret.name }}
  namespace: {{ .Values.mongodb.namespace }}
spec:
  encryptedData:
    password: {{ .Values.mongodb.secret.sealedValue }}
  template:
    metadata:
      name: {{ .Values.mongodb.secret.name }}
      namespace: {{ .Values.mongodb.secret.namespace }}
{{ end }}
