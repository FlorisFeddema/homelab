apiVersion: mongodbcommunity.mongodb.com/v1
kind: MongoDBCommunity
metadata:
  name: unifi-controller-database
  namespace: mongodb-community-operator
spec:
  members: 1
  type: ReplicaSet
  version: "6.0.5"
  security:
    authentication:
      modes: ["SCRAM"]
  users:
    - name: unificontroller
      db: unificontroller
      passwordSecretRef:
        name: unifi-controller-database-credentials
      roles:
        - name: dbOwner
          db: unificontroller
        - name: clusterAdmin
          db: unificontroller
        - name: userAdminAnyDatabase
          db: unificontroller
      scramCredentialsSecretName: unificontroller-scram
  additionalMongodConfig:
    storage.wiredTiger.engineConfig.journalCompressor: zlib
