node-feature-discovery:
  worker:
    config:
      usb:
        # https://www.usb.org/defined-class-codes
        deviceClassWhitelist: ["02", "03", "0e", "ef", "fe", "ff"]

  prometheus:
    enable: true
