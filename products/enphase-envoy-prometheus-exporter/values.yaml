enphase-envoy-prometheus-exporter:
  replicaCount: 1

  existingSecret: 'enphase-envoy-prometheus-exporter-credentials'

  resources:
    limits:
      memory: 128Mi
    requests:
      cpu: 20m
      memory: 16Mi
