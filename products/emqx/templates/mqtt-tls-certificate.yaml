apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: mqtt-tls-certificate
spec:
  secretName: mqtt-tls-certificate
  dnsNames:
    - 'mqtt.cloud.feddema.dev'
  issuerRef:
    name: letsencrypt-dns
    kind: ClusterIssuer
