apiVersion: gateway.envoyproxy.io/v1alpha1
kind: ClientTrafficPolicy
metadata:
  name: rook-ceph-objectstore-s3-traffic-policy
spec:
  targetRefs:
    - group: gateway.networking.k8s.io
      kind: HTTPRoute
      name: rook-ceph-objectstore-s3-route
  limits:
    maxRequestBodyBytes: 0
