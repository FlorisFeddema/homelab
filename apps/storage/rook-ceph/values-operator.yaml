rook-ceph:

  revisionHistoryLimit: 0
  csi:
    enableMetadata: true
    topology:
      enabled: true
      domainLabels: kubernetes.io/hostname,topology.kubernetes.io/zone
    enableLiveness: true
    serviceMonitor:
      enabled: true
  monitoring:
    enabled: true
