democratic-csi:
  csiDriver:
    name: org.democratic-csi.node-manual
    attachRequired: true
  controller:
    externalAttacher:
      enabled: true
    externalProvisioner:
      enabled: false
    externalResizer:
      enabled: false
    externalSnapshotter:
      enabled: false

  driver:
    config:
      driver: node-manual # required
