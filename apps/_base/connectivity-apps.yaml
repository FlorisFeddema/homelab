apps:
  cilium-gateway:
    deploy: true
  ingress-nginx:
    deploy: true
  adguard:
    deploy: true
    autoSyunc: false
  external-dns:
    deploy: true
  network-devices:
    deploy: true
    autoSync: false
