apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ template "homeAssistant.fullname" . }}-base-configuration
  labels:
    home-assistant-config: "1"
data:
{{ (.Files.Get "configuration/configuration.yaml") | indent 2 }}
