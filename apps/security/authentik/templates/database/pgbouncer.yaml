# apiVersion: postgresql.cnpg.io/v1
# kind: Pooler
# metadata:
#   name: authentik-db-pooler-rw
# spec:
#   cluster:
#     name: authentik-db
#   instances: 2
#   type: rw
#   pgbouncer:
#     poolMode: session
# ---
# apiVersion: monitoring.coreos.com/v1
# kind: PodMonitor
# metadata:
#   name: authentik-db-pooler-rw-monitor
# spec:
#   selector:
#     matchLabels:
#       "cnpg.io/poolerName": authentik-db-pooler-rw
#   podMetricsEndpoints:
#   - port: metrics
