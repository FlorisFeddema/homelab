node-feature-discovery:
  master:
    tolerations:
    - operator: Exists
  worker:
    tolerations:
    - operator: Exists
  gc:
    tolerations:
    - operator: Exists
  prometheus:
    enable: true
