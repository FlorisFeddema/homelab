# apiVersion: bitnami.com/v1alpha1
# kind: SealedSecret
# metadata:
#   name: route-oidc-secret
#   namespace: opentherm-monitor
# spec:
#   encryptedData:
#     # echo -n "client-secret" | kubeseal --cert https://sealed-secrets.mobrockers.com/v1/cert.pem --raw --name="route-oidc-secret" -n opentherm-monitor | clip.exe
#     client-secret: 
