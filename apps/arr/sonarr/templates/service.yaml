apiVersion: v1
kind: Service
metadata:
  name: sonarr
  labels:
    app.kubernetes.io/name: sonarr
    app.kubernetes.io/instance: sonarr
spec:
  ports:
    - name: http
      port: 7878
      targetPort: http
    - name: monitoring
      port: 9707
      targetPort: monitoring
    - name: additional-monitoring
      port: 9708
      targetPort: additional-monitoring
  selector:
    app.kubernetes.io/instance: sonarr
    app.kubernetes.io/name: sonarr
