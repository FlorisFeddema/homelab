apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: smartmeter-gateway-prometheus-exporter
  namespace: argocd
  finalizers: {}
#    - resources-finalizer.argocd.argoproj.io

spec:
  project: no-sync
  sources:
    - repoURL: git@github.com:FlorisFeddema/cloud-gitops.git
      targetRevision: main
      ref: repo
    - chart: smartmeter-gateway-prometheus-exporter
      repoURL: https://florisfeddema.github.io/smartmeter-gateway-prometheus-exporter/
      targetRevision: 0.1.1
      helm:
        valueFiles:
          - $repo/dev/smartmeter-gateway-prometheus-exporter/values.yaml
    - repoURL: git@github.com:FlorisFeddema/cloud-gitops.git
      targetRevision: HEAD
      path: dev/smartmeter-gateway-prometheus-exporter

  destination:
    server: https://kubernetes.default.svc
    namespace: smartmeter-gateway-prometheus-exporter

  revisionHistoryLimit: 2
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 5
        maxDuration: 10m
