name: Product build

on:
  workflow_call:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
#      - uses: actions/checkout@v3

      - name: Run a one-line script
        run: echo Hello from template
