apiVersion: v1
kind: Pod
metadata:
  name: copy-data
spec:
  nodeName: hortek-0
  restartPolicy: Never
  containers:
  - name: copy
    image: busybox
    command: ["sh", "-c", "cp -ar /mnt/src/. /mnt/dst/"]
    volumeMounts:
    - name: src
      mountPath: /mnt/src
    - name: dst
      mountPath: /mnt/dst
  volumes:
  - name: src
    persistentVolumeClaim:
      claimName: media-server-media
  - name: dst
    persistentVolumeClaim:
      claimName: tmp-pvc